#api calls are not actually made. UriComponentBuilders need properly formatted urls
customers.host.url=http://fakedomain:8081
orders.host.url=http://fakedomain:8082
tables.host.url=http://fakedomain:8083

customer.get-all.endpoint=/fake
customer.get-by-name.endpoint=/fake
customer.get-by-table.endpoint=/fake
customer.exists.endpoint=/fake
customer.seat.endpoint=/fake
customer.seat-group.endpoint=/fake
customer.boot.endpoint=/fake
order.submit.endpoint=/fake
order.serve.endpoint=/fake
table.get-all.endpoint=/fake