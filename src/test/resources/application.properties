#api calls are not actually made. UriComponentBuilders need properly formatted urls
customers.host.url=http://fakedomain:8081
orders.host.url=http://fakedomain:8082
tables.host.url=http://fakedomain:8083

customers.get-all.endpoint=/fake
customers.get-by-name.endpoint=/fake
customers.exists.endpoint=/fake
customers.seat.endpoint=/fake
customers.seat-group.endpoint=/fake
customers.boot.endpoint=/fake
orders.submit.endpoint=/fake
orders.serve.endpoint=/fake
tables.get-all.endpoint=/fake